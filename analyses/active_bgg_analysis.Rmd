# Historical BGG Analysis

Exploratory analysis on historical bgg rankings.

```{r global seetings, echo=F, warning=F, message=F}

knitr::opts_chunk$set(dev="png", fig.width = 8, fig.height = 7)
options(knitr.duplicate.label = "allow")

options(scipen=999)

```

```{r load and set packages, warning=F, message=F, include=FALSE, results = 'hide'}

source("load_packages.R")
source("theme_phil.R")

```

## Connect to Big Query

We'll first connect to the active table.

```{r connect to big query}

library(bigrquery)

# get project credentials
PROJECT_ID <- "gcp-analytics-326219"
BUCKET_NAME <- "test-bucket"

# set project and schema
bq_bgg<- bq_dataset(project = PROJECT_ID,
                                    dataset = 'bgg')

# query table
bgg_rankings<-bq_dataset_query(bq_bgg,
                        query = 'SELECT * FROM active_bgg_rankings_view',
                        quiet = F)

```


